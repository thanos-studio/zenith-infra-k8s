apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zenith_system.fullname" . }}-test-connection"
  labels:
    {{- include "zenith_system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zenith_system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
