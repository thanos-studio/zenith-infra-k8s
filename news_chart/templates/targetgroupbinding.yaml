{{- if .Values.targetGroupBinding.enabled }}
{{- $namespace := include "news_app.namespace" . -}}
{{- $binding := .Values.targetGroupBinding -}}
{{- $bindingName := default (printf "%s-tgb" (include "news_app.fullname" .)) $binding.name -}}
apiVersion: elbv2.k8s.aws/v1beta1
kind: TargetGroupBinding
metadata:
  name: {{ $bindingName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "news_app.labels" . | nindent 4 }}
spec:
  serviceRef:
    name: {{ include "news_app.activeServiceName" . }}
    port: {{ default .Values.service.port $binding.servicePort }}
  targetGroupARN: {{ required "targetGroupBinding.targetGroupARN is required" $binding.targetGroupARN }}
  targetType: {{ $binding.targetType }}
  {{- with $binding.networking }}
  networking:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
